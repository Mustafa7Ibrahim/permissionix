name: permissionix
description: A simple package to handle user permissions for accessing a feature
version: 0.1.0+1
homepage: https://github.com/Mustafa7Ibrahim/permissionix/blob/main/LICENSE
repository: https://github.com/Mustafa7Ibrahim/permissionix
issue_tracker: https://github.com/Mustafa7Ibrahim/permissionix/issues

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ">=6.0.0 <9.0.0"
